class Nodo:
    def __init__(self, v):
        self.valor= v
        self.prox = None
class ListaEnc:
    def __init__(self):
        self.inicio= None    
        self.fim = None
        self.tam = 0
    

def inserir(self, pos, va):
    if pos >= 0: 
        if self.tam == 0:
            aux = Nodo(va)
            self.ini = aux
            self.fim = aux
            self.tam += 1
        elif pos == self.tam:   
            aux= Nodo(va)
            self.fim.prox = aux
            self.fim = aux
            self.tam+=1
        elif pos == 0:
            aux== Nodo(va)
            aux.prox = self.ini
            self.ini = aux
            self.tam +=1
        else:
            aux = Nodo(va)
            var = self.ini
            for i in range (pos):
                anterior = var
                var = var.prox
            aux.prox = var
            anterior.prox = aux
            self.tam +=1
        return True 
    else:
        return False

def valor(self, pos):
    cont = 0 
    var = self.ini
    if var != None:
        while var.prox!= None:
            if cont == pos:
                return var.valor
            var = var.prox
            cont += 1



        def imprimir(self):
            var = self.ini
            while  var.prox != None:
                print(var.valor)
                var = var.prox
            if var.prox == None:
                print(var.valor)

lista = ListaEnc()
print(lista(inserir(0, 0, 'A')))
print(lista(inserir(1, 1, 'B')))
print(lista(inserir(2, 2, 'C')))
print(lista(inserir(0, 0, 'D')))
print(lista(inserir(2, 2, 'E')))
